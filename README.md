# User-Databese
Menu-driven Java console program that displays a menu to the user.

The menu displays the following options:

Add a user
Delete a user
Update a user - by prompting for email
List users - Displays all the users in multidimensional
